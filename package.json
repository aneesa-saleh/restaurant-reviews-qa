{
  "name": "restaurant-reviews-qa",
  "version": "1.0.0",
  "description": "QA Project for Restaurant Reviews Web App",
  "main": "index.js",
  "scripts": {
    "test": "npm run cypress:run",
    "cypress:open": "npx cypress open",
    "cypress:run": "npm run prereport && npx cypress run",
    "cypress:run:chrome": "npm run prereport && npx cypress run -b chrome",
    "cypress:run:firefox": "npm run prereport && npx cypress run -b firefox",
    "cypress:run:edge": "npm run prereport && npx cypress run -b edge",
    "junit:merge": "npx junit-merge -d \"cypress/results/junit\" -o \"cypress/results/junit/results.xml\"",
    "cypress:e2e": "npm run cypress:run; npm run junit:merge",
    "cypress:e2e:chrome": "npm run cypress:run:chrome; npm run junit:merge",
    "cypress:e2e:firefox": "npm run cypress:run:firefox; npm run junit:merge",
    "cypress:e2e:edge": "npm run cypress:run:edge; npm run junit:merge",
    "delete:reports": "rm -r cypress/results || true",
    "prereport": "npm run delete:reports",
    "serve": "serve -l 4300 app",
    "serve-and-test": "start-server-and-test serve http://localhost:4300 test",
    "serve-and-open-cypress": "start-server-and-test serve http://localhost:4300 cypress:open"
  },
  "keywords": [
    "qa"
  ],
  "author": "Aneesa Saleh",
  "license": "MIT",
  "dependencies": {
    "cypress": "^13.2.0"
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "start-server-and-test": "^2.0.1",
    "typescript": "^5.2.2"
  }
}
